# Optional. Only needed for local preview (bundle exec jekyll serve).
# GitHub Pages builds and deploys the site without any of this.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick" # required by Jekyll's local server on Ruby 3+
